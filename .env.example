# blivechat-tts 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# blivechat 认证令牌 (必需)
BLC_TOKEN=your_token_here

# blivechat 服务器端口 (可选，默认 12450)
BLC_PORT=12450

# blivechat 服务器主机 (可选，默认 localhost)
BLC_HOST=localhost

# 信号文件路径 (可选，用于 docker-compose 中的服务间通信)
# SIGNAL_FILE_PATH=/tmp/signal/config.txt
